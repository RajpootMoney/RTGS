/**
 * Next.js configuration for static export.
 */
module.exports = {
  // Enable React strict mode
  reactStrictMode: true,
  // Optional: customize trailingSlash for static hosting
  trailingSlash: true,
};
